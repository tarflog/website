---
layout: base.njk
nav: channels
title: Intercept channels — where the log surfaces first
permalink: /channels/
description: "Where fragments of the observation log surface before the registry: TikTok, YouTube, Instagram, X and Telegram, with the RSS feed and the address for correspondence."
---
<main class="wrap dossier">
  <div class="head">
    <h1>INTERCEPT CHANNELS · WHERE THE LOG APPEARS</h1>
  </div>
  <table>
    {% for c in site.channels %}
    <tr><th>{{ c.label }}</th><td><a class="btn" href="{{ c.url }}" target="_blank" rel="noopener me">{{ c.handle }}</a></td></tr>
    {% endfor %}
    <tr><th>SIGNAL ADDRESS</th><td>{{ site.email }}</td></tr>
    <tr><th>RSS</th><td><a class="btn" href="{{ '/feed.xml' | url }}">/feed.xml</a></td></tr>
  </table>
  <p class="selfnote">Fragments leak to these channels first. Letters are rare and concern declassifications only.</p>
</main>
